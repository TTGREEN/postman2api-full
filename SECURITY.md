# 安全策略

## 支持范围

当前仅维护 `main` 分支最新提交。完整包属于交付产物，不作为安全修复的主线来源。

## 请不要公开提交

- `.env`、`.env.*`
- `data/`、SQLite 数据库、迁移后的运行数据
- `runtime/`、浏览器缓存、用户 profile
- `tokens/`、Cookie、Postman session、API Key、账号密码
- 日志全文、截图中包含的账号或 Token

## 报告安全问题

如果发现敏感信息泄露、鉴权绕过、管理接口暴露、SSRF、任意文件读写或账号数据风险，请通过私有渠道联系仓库维护者。公开 Issue 只应包含脱敏后的现象和复现范围。

## 默认部署边界

本服务默认面向本地或私有网络。不要在未加反向代理鉴权、TLS、访问控制和强随机 `API_KEY` / `ENCRYPTION_KEY` 的情况下暴露到公网。

## 贡献要求

安全相关 PR 需要说明：

- 改动影响的入口和数据边界
- 是否涉及账号凭据、Cookie、Token、数据库或日志
- 已运行的验证命令
- 是否需要用户迁移配置或轮换密钥
